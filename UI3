--// KilometresUI V6
--// Modern Dashboard UI Framework
--// Single File Architecture
--// Inspired by Rayfield + Fluent + Dashboard Systems
--// Mobile Optimized + Executor Friendly
--// By ChatGPT for Kilometres

--// SERVICES

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local HttpService = game:GetService("HttpService")
local Lighting = game:GetService("Lighting")

local LocalPlayer = Players.LocalPlayer

--// ROOT

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "KilometresUI_V6"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Global
pcall(function()
    ScreenGui.Parent = CoreGui
end)

--// BLUR

local Blur = Lighting:FindFirstChild("KilometresBlur")
if not Blur then
    Blur = Instance.new("BlurEffect")
    Blur.Name = "KilometresBlur"
    Blur.Size = 0
    Blur.Parent = Lighting
end

TweenService:Create(
    Blur,
    TweenInfo.new(0.4, Enum.EasingStyle.Quint),
    {Size = 18}
):Play()

--// CONFIG

local Config = {
    Theme = "Blueprint",
    Blur = true,
    FPS = true,
    Ping = true,
    Compact = false,
}

--// THEMES

local Themes = {
    Blueprint = {
        Accent = Color3.fromRGB(0,170,255),
        Background = Color3.fromRGB(16,18,24),
        Light = Color3.fromRGB(28,32,40),
        Stroke = Color3.fromRGB(70,90,120),
        Text = Color3.fromRGB(255,255,255),
    },

    Crimson = {
        Accent = Color3.fromRGB(255,70,70),
        Background = Color3.fromRGB(20,16,16),
        Light = Color3.fromRGB(40,24,24),
        Stroke = Color3.fromRGB(100,40,40),
        Text = Color3.fromRGB(255,255,255),
    },

    AMOLED = {
        Accent = Color3.fromRGB(255,255,255),
        Background = Color3.fromRGB(0,0,0),
        Light = Color3.fromRGB(15,15,15),
        Stroke = Color3.fromRGB(40,40,40),
        Text = Color3.fromRGB(255,255,255),
    }
}

local Theme = Themes[Config.Theme]

--// UTIL

local function Tween(obj, props, time)
    TweenService:Create(
        obj,
        TweenInfo.new(
            time or 0.25,
            Enum.EasingStyle.Quint,
            Enum.EasingDirection.Out
        ),
        props
    ):Play()
end

local function Round(obj, radius)
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0,radius)
    c.Parent = obj
end

local function Stroke(obj, color)
    local s = Instance.new("UIStroke")
    s.Color = color
    s.Thickness = 1
    s.Transparency = 0.3
    s.Parent = obj
end

local function Dragify(frame)
    local dragToggle = nil
    local dragInput = nil
    local dragStart = nil
    local startPos = nil

    local function update(input)
        local delta = input.Position - dragStart
        frame.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end

    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then
            dragToggle = true
            dragStart = input.Position
            startPos = frame.Position

            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragToggle = false
                end
            end)
        end
    end)

    frame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement
        or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)

    UIS.InputChanged:Connect(function(input)
        if input == dragInput and dragToggle then
            update(input)
        end
    end)
end

--// MAIN WINDOW

local Main = Instance.new("Frame")
Main.Size = UDim2.new(0,760,0,500)
Main.Position = UDim2.new(0.5,-380,0.5,-250)
Main.BackgroundColor3 = Theme.Background
Main.Parent = ScreenGui
Round(Main,18)
Stroke(Main,Theme.Stroke)
Dragify(Main)

--// SHADOW

local Shadow = Instance.new("ImageLabel")
Shadow.Size = UDim2.new(1,60,1,60)
Shadow.Position = UDim2.new(0,-30,0,-30)
Shadow.BackgroundTransparency = 1
Shadow.Image = "rbxassetid://1316045217"
Shadow.ImageTransparency = 0.7
Shadow.ScaleType = Enum.ScaleType.Slice
Shadow.SliceCenter = Rect.new(10,10,118,118)
Shadow.ZIndex = 0
Shadow.Parent = Main

--// TOPBAR

local Topbar = Instance.new("Frame")
Topbar.Size = UDim2.new(1,0,0,54)
Topbar.BackgroundTransparency = 1
Topbar.Parent = Main

local Title = Instance.new("TextLabel")
Title.Text = "KilometresUI V6"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 18
Title.TextColor3 = Theme.Text
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0,18,0,0)
Title.Size = UDim2.new(0,220,1,0)
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Topbar

--// BUTTONS

local HideButton = Instance.new("TextButton")
HideButton.Size = UDim2.new(0,36,0,36)
HideButton.Position = UDim2.new(1,-50,0.5,-18)
HideButton.Text = "-"
HideButton.Font = Enum.Font.GothamBold
HideButton.TextSize = 20
HideButton.TextColor3 = Theme.Text
HideButton.BackgroundColor3 = Theme.Light
HideButton.Parent = Topbar
Round(HideButton,12)

local SettingsButton = Instance.new("TextButton")
SettingsButton.Size = UDim2.new(0,36,0,36)
SettingsButton.Position = UDim2.new(1,-92,0.5,-18)
SettingsButton.Text = "⚙"
SettingsButton.Font = Enum.Font.GothamBold
SettingsButton.TextSize = 18
SettingsButton.TextColor3 = Theme.Text
SettingsButton.BackgroundColor3 = Theme.Light
SettingsButton.Parent = Topbar
Round(SettingsButton,12)

--// SIDEBAR

local Sidebar = Instance.new("Frame")
Sidebar.Size = UDim2.new(0,170,1,-54)
Sidebar.Position = UDim2.new(0,0,0,54)
Sidebar.BackgroundTransparency = 1
Sidebar.Parent = Main

local TabLayout = Instance.new("UIListLayout")
TabLayout.Padding = UDim.new(0,8)
TabLayout.Parent = Sidebar

--// CONTENT

local Content = Instance.new("Frame")
Content.Size = UDim2.new(1,-190,1,-74)
Content.Position = UDim2.new(0,180,0,64)
Content.BackgroundTransparency = 1
Content.Parent = Main

--// TABS

local Tabs = {}
local CurrentTab = nil

local function CreateTab(name)
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(1,-16,0,42)
    Button.Position = UDim2.new(0,8,0,0)
    Button.Text = name
    Button.Font = Enum.Font.GothamSemibold
    Button.TextSize = 14
    Button.TextColor3 = Theme.Text
    Button.BackgroundColor3 = Theme.Light
    Button.Parent = Sidebar
    Round(Button,12)

    local Frame = Instance.new("ScrollingFrame")
    Frame.Size = UDim2.new(1,0,1,0)
    Frame.CanvasSize = UDim2.new(0,0,0,1000)
    Frame.ScrollBarThickness = 0
    Frame.Visible = false
    Frame.BackgroundTransparency = 1
    Frame.Parent = Content

    local Layout = Instance.new("UIListLayout")
    Layout.Padding = UDim.new(0,12)
    Layout.Parent = Frame

    Button.MouseButton1Click:Connect(function()
        for _,v in pairs(Tabs) do
            v.Frame.Visible = false
        end

        Frame.Visible = true
        CurrentTab = Frame
    end)

    Tabs[name] = {
        Button = Button,
        Frame = Frame
    }

    return Frame
end

--// HOME TAB

local Home = CreateTab("Home")
Home.Visible = true

--// SECTION FUNCTION

local function CreateSection(parent, title)
    local Section = Instance.new("Frame")
    Section.Size = UDim2.new(1,-10,0,220)
    Section.BackgroundColor3 = Theme.Light
    Section.Parent = parent
    Round(Section,16)
    Stroke(Section,Theme.Stroke)

    local Label = Instance.new("TextLabel")
    Label.Text = title
    Label.Font = Enum.Font.GothamBold
    Label.TextSize = 15
    Label.TextColor3 = Theme.Text
    Label.BackgroundTransparency = 1
    Label.Position = UDim2.new(0,16,0,10)
    Label.Size = UDim2.new(1,-20,0,24)
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Parent = Section

    return Section
end

local Dashboard = CreateSection(Home,"Dashboard")

--// TOGGLE

local function CreateToggle(parent, text, default, callback)
    local Toggle = Instance.new("TextButton")
    Toggle.Size = UDim2.new(1,-20,0,40)
    Toggle.Position = UDim2.new(0,10,0,40)
    Toggle.BackgroundColor3 = Theme.Background
    Toggle.Text = ""
    Toggle.Parent = parent
    Round(Toggle,12)

    local Label = Instance.new("TextLabel")
    Label.Text = text
    Label.Font = Enum.Font.Gotham
    Label.TextSize = 14
    Label.TextColor3 = Theme.Text
    Label.BackgroundTransparency = 1
    Label.Position = UDim2.new(0,12,0,0)
    Label.Size = UDim2.new(1,-70,1,0)
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Parent = Toggle

    local Circle = Instance.new("Frame")
    Circle.Size = UDim2.new(0,22,0,22)
    Circle.Position = UDim2.new(1,-34,0.5,-11)
    Circle.BackgroundColor3 = default and Theme.Accent or Color3.fromRGB(80,80,80)
    Circle.Parent = Toggle
    Round(Circle,999)

    local State = default

    Toggle.MouseButton1Click:Connect(function()
        State = not State

        Tween(Circle,{
            BackgroundColor3 = State and Theme.Accent or Color3.fromRGB(80,80,80)
        },0.25)

        callback(State)
    end)
end

CreateToggle(Dashboard,"Enable Blur",true,function(v)
    Config.Blur = v

    if v then
        Tween(Blur,{Size = 18},0.4)
    else
        Tween(Blur,{Size = 0},0.4)
    end
end)

--// WIDGETS

local FPSWidget = Instance.new("Frame")
FPSWidget.Size = UDim2.new(0,120,0,50)
FPSWidget.Position = UDim2.new(0,20,0,100)
FPSWidget.BackgroundColor3 = Theme.Light
FPSWidget.Parent = ScreenGui
Round(FPSWidget,14)
Stroke(FPSWidget,Theme.Stroke)
Dragify(FPSWidget)

local FPSLabel = Instance.new("TextLabel")
FPSLabel.Size = UDim2.new(1,0,1,0)
FPSLabel.BackgroundTransparency = 1
FPSLabel.TextColor3 = Theme.Text
FPSLabel.Font = Enum.Font.GothamBold
FPSLabel.TextSize = 15
FPSLabel.Text = "FPS: 0"
FPSLabel.Parent = FPSWidget

--// FPS COUNTER

local FPS = 0
local Last = tick()

RunService.RenderStepped:Connect(function()
    FPS += 1

    if tick() - Last >= 1 then
        FPSLabel.Text = "FPS: "..FPS
        FPS = 0
        Last = tick()
    end
end)

--// SETTINGS PANEL

local SettingsPanel = Instance.new("Frame")
SettingsPanel.Size = UDim2.new(0,260,0,340)
SettingsPanel.Position = UDim2.new(1,-280,0,60)
SettingsPanel.BackgroundColor3 = Theme.Background
SettingsPanel.Visible = false
SettingsPanel.Parent = Main
Round(SettingsPanel,16)
Stroke(SettingsPanel,Theme.Stroke)

local SettingsTitle = Instance.new("TextLabel")
SettingsTitle.Text = "Settings"
SettingsTitle.Font = Enum.Font.GothamBold
SettingsTitle.TextSize = 16
SettingsTitle.TextColor3 = Theme.Text
SettingsTitle.BackgroundTransparency = 1
SettingsTitle.Size = UDim2.new(1,0,0,40)
SettingsTitle.Parent = SettingsPanel

SettingsButton.MouseButton1Click:Connect(function()
    SettingsPanel.Visible = not SettingsPanel.Visible

    if SettingsPanel.Visible then
        SettingsPanel.Size = UDim2.new(0,0,0,0)

        Tween(SettingsPanel,{
            Size = UDim2.new(0,260,0,340)
        },0.3)
    end
end)

--// THEME BUTTONS

local ThemeY = 60

for ThemeName,_ in pairs(Themes) do
    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(1,-20,0,36)
    Btn.Position = UDim2.new(0,10,0,ThemeY)
    Btn.BackgroundColor3 = Theme.Light
    Btn.Text = ThemeName
    Btn.Font = Enum.Font.GothamSemibold
    Btn.TextSize = 14
    Btn.TextColor3 = Theme.Text
    Btn.Parent = SettingsPanel
    Round(Btn,12)

    ThemeY += 46

    Btn.MouseButton1Click:Connect(function()
        local T = Themes[ThemeName]

        Main.BackgroundColor3 = T.Background
        FPSWidget.BackgroundColor3 = T.Light

        Theme = T
    end)
end

--// HIDE SYSTEM

local Hidden = false

HideButton.MouseButton1Click:Connect(function()
    Hidden = not Hidden

    if Hidden then
        Tween(Main,{
            Size = UDim2.new(0,760,0,54)
        },0.3)

        Tween(Blur,{
            Size = 0
        },0.3)
    else
        Tween(Main,{
            Size = UDim2.new(0,760,0,500)
        },0.3)

        if Config.Blur then
            Tween(Blur,{
                Size = 18
            },0.3)
        end
    end
end)

--// NOTIFY

local Notify = Instance.new("Frame")
Notify.Size = UDim2.new(0,260,0,70)
Notify.Position = UDim2.new(1,-280,1,-90)
Notify.BackgroundColor3 = Theme.Light
Notify.Parent = ScreenGui
Round(Notify,16)
Stroke(Notify,Theme.Stroke)

local NotifyText = Instance.new("TextLabel")
NotifyText.Size = UDim2.new(1,0,1,0)
NotifyText.BackgroundTransparency = 1
NotifyText.Text = "KilometresUI Loaded"
NotifyText.Font = Enum.Font.GothamBold
NotifyText.TextSize = 15
NotifyText.TextColor3 = Theme.Text
NotifyText.Parent = Notify

Notify.Position = UDim2.new(1,300,1,-90)

Tween(Notify,{
    Position = UDim2.new(1,-280,1,-90)
},0.4)

task.wait(3)

Tween(Notify,{
    Position = UDim2.new(1,300,1,-90)
},0.4)

--// CHAT WINDOW

local ChatWindow = Instance.new("Frame")
ChatWindow.Size = UDim2.new(0,320,0,220)
ChatWindow.Position = UDim2.new(1,-340,1,-260)
ChatWindow.BackgroundColor3 = Theme.Background
ChatWindow.Parent = ScreenGui
Round(ChatWindow,16)
Stroke(ChatWindow,Theme.Stroke)
Dragify(ChatWindow)

local ChatInput = Instance.new("TextBox")
ChatInput.Size = UDim2.new(1,-20,0,40)
ChatInput.Position = UDim2.new(0,10,1,-50)
ChatInput.BackgroundColor3 = Theme.Light
ChatInput.TextColor3 = Theme.Text
ChatInput.PlaceholderText = "Type message..."
ChatInput.Font = Enum.Font.Gotham
ChatInput.TextSize = 14
ChatInput.Parent = ChatWindow
Round(ChatInput,12)

local Send = Instance.new("TextButton")
Send.Size = UDim2.new(0,70,0,40)
Send.Position = UDim2.new(1,-80,1,-50)
Send.BackgroundColor3 = Theme.Accent
Send.Text = "Send"
Send.Font = Enum.Font.GothamBold
Send.TextSize = 14
Send.TextColor3 = Color3.new(1,1,1)
Send.Parent = ChatWindow
Round(Send,12)

Send.MouseButton1Click:Connect(function()
    pcall(function()
        game.ReplicatedStorage.DefaultChatSystemChatEvents
        .SayMessageRequest:FireServer(ChatInput.Text,"All")
    end)
end)

--// KEYBIND

UIS.InputBegan:Connect(function(input,gp)
    if gp then return end

    if input.KeyCode == Enum.KeyCode.RightControl then
        ScreenGui.Enabled = not ScreenGui.Enabled
    end
end)

print("KilometresUI V6 Loaded")
